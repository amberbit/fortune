[{kernel,
  [
    {distributed, [{ui, 5000, [ui@localhost]}, {backend, 5000, [backend@localhost, ui@localhost]}]},
    {sync_nodes_mandatory, ['ui@localhost']},
    {sync_nodes_timeout, 10000}
  ]}
].
